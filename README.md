# timon's devenv and nix templates

Usage

1. `nix flake init -t github:timonviola/devenv-templates#python`
2. `devenv init` # follow instructions
3. `direnv allow`



Should you use it?

Probably not as I am still figuring out the ins-n-outs of nix and devenv.
